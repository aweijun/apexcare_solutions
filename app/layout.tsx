import './globals.css'
import { Montserrat } from 'next/font/google'

const montserrat = Montserrat({ subsets: ['latin'] })

export const metadata = {
  title: 'ApexCare Medical Solutions',
  description: 'Premium quality medical supplies & equipment at competitive prices',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={`${montserrat.className} bg-white text-gray-800`}>{children}</body>
    </html>
  )
}

